# Plugin configuration

Copy the relevant `plugins:` section from `analysis_options.yaml` into the
application's top-level `analysis_options.yaml` and restart the Dart Analysis
Server.


During local plugin development, replace the version with an absolute `path:`
entry. Published applications should use the hosted version constraint.
